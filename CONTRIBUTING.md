# 贡献指南

感谢你参与 MathArts。本文件是组织级默认贡献入口；目标仓库存在本地贡献指南时，请同时遵守其中的项目特定要求。

## 1. 选择正确的入口

| 事项 | 入口 |
| --- | --- |
| 可复现的 Bug | 目标仓库的 Bug Issue Form |
| 明确的功能或改进建议 | 目标仓库的 Feature Issue Form |
| 文档、术语或知识数据问题 | 目标仓库的 Docs/Data Issue Form |
| 开放问题、方向讨论或跨仓库想法 | [MathArts Discussions](https://github.com/orgs/matharts/discussions) |
| 安全漏洞 | 按[安全策略](SECURITY.md)私密报告 |
| 社区行为问题 | 按[社区行为准则](CODE_OF_CONDUCT.md)私密报告 |

请先搜索已有 Issue、Pull Request 和 Discussion，避免分散同一问题的证据与讨论。

## 2. 是否需要先讨论

以下低风险变更通常可以直接提交 Pull Request：

- 错别字、失效链接和局部文档澄清
- 范围明确的小型 Bug 修复
- 不改变外部行为的测试补充或内部整理
- 已有 Issue 明确接受范围的实现

以下变更应先建立 Issue 或 Discussion：

- 新公共 API、配置或数据格式
- Breaking Change、弃用或迁移
- 可能改变计算结果的术语、规则或知识数据
- 新依赖、新服务或持续维护成本
- 跨仓库约定、组织政策或权限边界
- 大范围重构或存在多种可行方案的设计

高影响变更还应遵循[组织治理](GOVERNANCE.md)规定的决策层级和提案要求。

## 3. 准备修改

1. 阅读目标仓库的 README、贡献指南和相关设计文档
2. 确认工作区状态，保留无关的本地修改
3. 使用目标仓库已经选择的语言、工具链和包管理器
4. 建立范围单一、名称清晰的工作分支
5. 先找到能够证明变更正确性的最小测试或检查

本组织不规定一套适用于所有项目的安装、构建或测试命令；目标仓库的说明是这些命令的权威来源。

## 4. 修改原则

- 一个 Pull Request 解决一个主要问题
- 不混入无关重构、格式化或依赖升级
- 优先修复根因，并保持公共接口尽可能简单
- 新行为应有测试；无法自动测试时说明人工验证方式
- 行为、接口、配置或数据变化应同步更新文档
- 不提交令牌、私钥、生产数据、个人数据或未公开漏洞信息
- 不覆盖或丢弃他人的未提交修改

## 5. 术语、规则与知识数据

涉及历法、术语、流派、映射、常量或推演规则时，请在 Issue 或 Pull Request 中说明：

- 当前内容与建议内容
- 资料来源和可定位引用
- 适用范围、前提与采用的传统或实现
- 已知差异、争议和不确定性
- 是否会改变现有计算结果或持久化数据
- 使用了什么测试、对照样例或人工核验

请区分原始资料、后续解释、项目约定和软件实现。引用来源不表示项目必须接受其中的全部结论。

## 6. Commit

Commit 应保持可审查，并使用简短的命令式摘要。推荐使用 Conventional Commits 风格：

```text
type(scope): summary
```

常见类型包括 `feat`、`fix`、`docs`、`test`、`refactor`、`build`、`ci` 和 `chore`。目标仓库可以根据发布工具制定更具体的要求。

不要为了制造整洁历史而改写他人已经依赖的公共历史。

## 7. Pull Request

Pull Request 应说明：

- 解决的问题和采用的方案
- 主要修改及明确的非目标
- 实际执行的测试和结果
- 公共接口、兼容性、安全或数据影响
- 需要审查者重点确认的内容

Draft Pull Request 适合分享尚未就绪但需要早期反馈的工作。就绪后再请求正式评审。

维护者可以要求拆分范围、补充测试、澄清来源或调整设计。提交者负责处理反馈；维护者负责说明阻塞合并的具体原因。

## 8. 合并条件

变更至少应满足：

- 范围和责任边界清楚
- 必要测试与检查通过
- 文档与实际行为一致
- 评审意见已解决或明确记录
- 不包含已知秘密、未公开漏洞或无关文件
- Breaking Change 具有迁移说明

合并方式、批准人数和发布要求由目标仓库的规则集与本地治理决定。

## 9. 自动化与 AI 辅助

可以使用自动化或 AI 辅助研究、编写和评审，但提交者仍对结果负责。请验证生成内容、许可、引用、测试和安全边界。

当自动化显著影响方案、生成大量内容或存在审查风险时，应在 Pull Request 中说明使用方式。不得把秘密、私人报告或无权共享的内容发送给外部服务。

## 10. 修改组织级文件

修改 `matharts/.github` 可能影响多个仓库。Pull Request 必须说明：

- 哪些仓库和贡献入口会受到影响
- 默认文件与项目本地覆盖之间的关系
- 自动化所需权限和失败模式
- 验证方式、迁移步骤和恢复路径

组织级规则只在本仓库维护；项目特定内容应留在目标项目。
