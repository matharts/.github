# 贡献指南

感谢你参与 MathArts。

MathArts 致力于建设中国数术领域的开放计算基础设施、公共标准体系和应用生态。我们欢迎代码、文档、测试、知识数据、规范讨论、问题报告和代码评审等不同形式的贡献。

本文件是 MathArts 组织级默认贡献指南。组织内仓库没有自己的 `CONTRIBUTING.md` 时，适用本指南；项目仓库提供了独立贡献指南时，以项目仓库的规定为准。

## 1. 开始之前

参与贡献前，请先：

1. 阅读目标仓库的 `README.md`，了解项目用途、成熟度和支持范围。
2. 阅读目标仓库自己的贡献指南、架构文档、规范和开发说明（如有）。
3. 搜索已有 Issues、Pull Requests 和 Discussions，避免重复报告或重复开发。
4. 阅读 MathArts 的[社区行为准则](https://github.com/matharts/.github/blob/main/CODE_OF_CONDUCT.md)。
5. 安全漏洞必须按照[安全策略](https://github.com/matharts/.github/blob/main/SECURITY.md)私密报告，不得提交公开 Issue。

你不需要先理解 MathArts 的全部组织结构，才能提交一个小型贡献。错别字、失效链接、局部文档澄清和范围明确的小型修复通常可以直接提交 Pull Request。

## 2. 参与方式

你可以通过以下方式参与：

* 报告可以复现的 Bug；
* 提出功能或改进建议；
* 修正文档、示例和失效链接；
* 增加测试和边界用例；
* 修复代码或构建流程；
* 改进开发者体验；
* 补充术语、资料来源和知识数据；
* 参与 Issue、Pull Request、RFC 或 Specification 的讨论；
* 帮助复现问题、验证修复或评审贡献。

非代码贡献与代码贡献同样重要。

## 3. 选择正确的入口

### 3.1 Bug

当项目行为与已有文档、测试、接口约定或合理预期不一致时，请在受影响的仓库提交 Bug Issue。

报告前应当：

* 搜索是否已有相同问题；
* 提供能够复现问题的最小示例；
* 提供项目版本、Commit 或发布版本；
* 提供必要的运行环境和错误信息；
* 删除日志中的令牌、密钥、个人数据和其他敏感信息。

涉及计算结果时，还应尽可能说明：

* 输入数据；
* 实际结果；
* 预期结果；
* 判断预期结果的依据；
* 采用的历法、流派、规则版本或其他前提。

无法确定问题属于实现错误、规范歧义还是理论分歧时，可以先提交问题，但必须清楚标注尚未确认的部分。

### 3.2 功能建议

功能建议应优先说明需要解决的问题，而不只是描述希望增加的实现。

建议包含：

* 目标用户；
* 使用场景；
* 当前工作流的限制；
* 期望结果；
* 可能的兼容性影响；
* 已考虑的替代方案；
* 涉及数术规则时的资料来源和适用范围。

提出建议不代表功能已经被接受，也不代表维护者承诺实现。

### 3.3 开放式讨论

以下内容适合先在 [MathArts Discussions](https://github.com/orgs/matharts/discussions) 中讨论：

* 尚未形成明确需求的问题；
* 理论、术语或流派差异；
* 项目方向建议；
* 跨仓库协作建议；
* 需要收集不同使用场景的提案；
* 尚不能判断应属于哪个仓库的想法。

讨论形成可执行范围后，再在具体仓库建立 Issue。

### 3.4 安全问题

可能造成未授权访问、敏感信息泄露、供应链风险、权限绕过或其他安全影响的问题，必须按照[安全策略](https://github.com/matharts/.github/blob/main/SECURITY.md)私密报告。

不得通过公开 Issue、Pull Request 或 Discussion 披露尚未修复的安全漏洞。

### 3.5 社区行为问题

骚扰、威胁、歧视、人身攻击或其他社区行为问题，应按照[社区行为准则](https://github.com/matharts/.github/blob/main/CODE_OF_CONDUCT.md)报告，不应作为普通技术 Issue 处理。

## 4. 是否需要先讨论

### 4.1 通常可以直接提交

以下改动通常不要求事先获得批准：

* 修复错别字或失效链接；
* 改善不改变原意的文档表述；
* 增加缺失的测试；
* 修复范围明确且已有 Issue 的 Bug；
* 与现有约定一致的小型维护改动；
* 不改变外部行为的局部代码清理。

即使可以直接提交，也应先搜索现有工作，避免与正在进行的 Pull Request 冲突。

### 4.2 应当先建立 Issue 或 Discussion

以下改动应当在开始大量实现前先讨论：

* 新增重要功能或公共能力；
* 修改公共 API、配置格式或数据格式；
* 引入新的运行时依赖；
* 大范围重构或文件迁移；
* 修改核心数据模型；
* 改变已有数术规则或计算结果；
* 修改术语的正式定义；
* 增加或修改规范性要求；
* 跨多个仓库的变更；
* 影响发布、兼容性或安全边界的变更；
* 删除、弃用或替换现有能力；
* 预计需要较大维护或评审成本的工作。

先讨论的目标是确认问题、范围和约束，而不是要求贡献者提前完成全部设计。

### 4.3 RFC

普通 Bug 修复和局部改进不需要 RFC。

只有在以下情况之一成立时，才应使用 RFC：

* 目标仓库已经建立 RFC 流程，并规定该类变更必须提交 RFC；
* 维护者在 Issue 或 Discussion 中明确要求提交 RFC；
* 变更影响多个实现、公共规范、长期兼容性或组织级协作方式，普通 Issue 无法充分承载决策。

提案在正式接受前必须保持“提案”状态，不得描述为已经生效的组织政策或技术标准。

### 4.4 ADR

ADR 用于记录已经作出的架构决定，不用于代替提案讨论。

不得通过直接提交 ADR 宣布某项尚未接受的技术方案。是否需要 ADR，应由目标仓库维护者根据已经完成的决策判断。

## 5. 认领任务

准备处理已有 Issue 时，请先查看：

* 是否已经有人留言认领；
* 是否存在相关的活跃 Pull Request；
* Issue 是否仍然有效；
* 是否有尚未解决的设计问题。

准备处理较大任务时，建议在 Issue 中说明：

* 你计划处理该问题；
* 预计修改的范围；
* 初步实现思路；
* 尚需确认的问题；
* 是否会修改公共接口、知识数据或文档。

对于小型、明确的修复，可以直接开始并提交 Pull Request。

Issue 中的认领不是永久保留权，也不代表 Pull Request 一定会被合并。如果无法继续，请在 Issue 中说明，方便其他贡献者接手。

不得在没有沟通的情况下重复实现已有活跃 Pull Request 正在处理的相同内容。

## 6. 准备开发环境

每个仓库的技术栈可能不同。具体安装、构建、测试和格式化命令，以目标仓库的文档和配置为准。

开始修改前，应当确认：

* 使用了项目要求的运行时和包管理器版本；
* 没有绕过仓库已有的锁文件；
* 能够在未修改代码时运行现有测试；
* 已了解仓库的格式化、Lint、类型检查和代码生成流程；
* 没有将本地密钥、环境变量或私有配置提交到仓库。

如果仓库缺少可执行的开发说明，可以提交文档改进，或在 Issue 中说明具体阻塞点。

## 7. Fork 和工作分支

通常可以按照以下流程贡献：

```bash
git clone https://github.com/<你的账号>/<仓库名>.git
cd <仓库名>

git remote add upstream https://github.com/matharts/<仓库名>.git
git fetch upstream
```

从目标仓库当前的默认分支创建工作分支。不得假设所有仓库的默认分支都叫 `main`。

```bash
git switch <默认分支>
git pull --ff-only upstream <默认分支>
git switch -c <类型>/<简要描述>
```

建议使用以下分支前缀：

| 类型    | 格式              | 示例                         |
| ----- | --------------- | -------------------------- |
| 新功能   | `feat/<描述>`     | `feat/bagua-sequence`      |
| 修复    | `fix/<描述>`      | `fix/tiangan-mapping`      |
| 文档    | `docs/<描述>`     | `docs/contributing-guide`  |
| 重构    | `refactor/<描述>` | `refactor/calendar-engine` |
| 测试    | `test/<描述>`     | `test/boundary-cases`      |
| 数据    | `data/<描述>`     | `data/glossary-source`     |
| 构建或维护 | `chore/<描述>`    | `chore/update-tooling`     |

分支名称应简短、可识别，并且不得包含个人信息或敏感信息。

## 8. 修改原则

### 8.1 保持范围集中

一个 Pull Request 应当解决一个主要问题。

不得在修复 Bug 的同时混入无关重构、批量格式化、依赖升级或大范围文件移动。确有必要时，应在 Pull Request 描述中解释原因。

### 8.2 尊重仓库边界

贡献前应理解目标仓库的职责。

项目特定的架构、实现和开发说明应留在项目仓库。组织级贡献政策和社区健康文件应由 `.github` 仓库或其他明确指定的治理仓库维护。

当职责边界不清楚时，应先建立 Issue 或 Discussion。

### 8.3 不用文档代替技术决策

文档应记录已经确认的行为、规则和决策。

存在未解决的架构、算法或规范争议时，应通过 Issue、RFC、测试和技术评审解决。不得通过修改文档把某一方案描述为既定事实。

### 8.4 避免无关修改

除非当前任务确实需要，否则不要：

* 批量重命名无关符号；
* 修改无关格式；
* 升级全部依赖；
* 重写整个文件；
* 更换构建工具；
* 改变公共接口；
* 删除兼容代码。

范围较小的 Pull Request 更容易审查、验证和回退。

## 9. 代码要求

代码贡献应当：

* 符合目标仓库的语言和代码风格；
* 优先使用仓库已有的格式化和检查工具；
* 保持命名清晰；
* 对非显而易见的逻辑解释“为什么”；
* 处理必要的边界情况和错误路径；
* 避免引入不必要的依赖；
* 不包含调试输出、密钥、凭证或私有数据；
* 不无故降低类型安全或测试覆盖；
* 与已接受的架构决定和规范保持一致。

通用约定参见 MathArts 的[代码风格指南](https://github.com/matharts/ecosystem/blob/main/specs/code/style.md)。项目仓库的本地规则优先于组织级通用指南。

## 10. 术语、规则和知识数据

涉及术语、典籍、流派、历法、映射关系、推演规则或其他知识数据时，应特别注意可追溯性和适用范围。

贡献者应尽可能说明：

* 中文原名；
* 代码标识符；
* 定义或规则；
* 资料来源；
* 适用流派或上下文；
* 已知歧义；
* 与现有实现的差异；
* 对计算结果和兼容性的影响。

### 10.1 资料来源

资料来源可以包括：

* 可定位的典籍原文；
* 已公开的学术资料；
* 已接受的项目规范；
* 已有实现及其文档；
* 明确标记的现代整理或工程约定。

仅写“传统如此”“通常认为”或“网上资料”通常不足以支持规范性变更。

不得把单一流派、单一实现或未经验证的整理描述为普遍共识、官方规则或行业标准。

### 10.2 不确定性

存在争议或资料不足时，应明确区分：

* 已验证事实；
* 当前实现；
* 已接受规范；
* 提案；
* 假设；
* 待验证内容；
* 流派差异。

不得为了让文档显得完整而隐藏不确定性。

### 10.3 数据变更

知识数据变更应与普通代码变更接受同等审查。

修改映射表、常量、规则表或典籍数据时，应尽可能提供：

* 变更前后的差异；
* 资料来源；
* 验证方式；
* 受影响的测试；
* 是否改变已有输出；
* 是否需要迁移说明。

## 11. 测试

所有可能改变行为的贡献都应提供适当验证。

根据改动类型，可以包括：

* 单元测试；
* 集成测试；
* 回归测试；
* 类型检查；
* Lint；
* 格式检查；
* 构建验证；
* 文档链接检查；
* 示例运行；
* 数据一致性检查；
* 与已知结果或参考实现的对照。

修复 Bug 时，建议先增加能够暴露问题的回归测试，再提交修复。

无法增加自动化测试时，应在 Pull Request 中说明原因，并提供可复现的人工验证步骤。

不得为了让 CI 通过而删除有效测试、放宽关键断言或隐藏错误。

## 12. 文档同步

当改动影响以下内容时，必须同步更新相应文档：

* 安装或开发命令；
* 公共 API；
* 配置项；
* 数据格式；
* 默认行为；
* 兼容性；
* 废弃内容；
* 用户可见输出；
* 数术术语或规则；
* 架构边界；
* 发布和迁移方式。

README 应用于项目介绍和快速开始，不应成为所有技术细节的唯一存放位置。

与代码行为直接相关的文档应尽量与代码在同一个 Pull Request 中更新。

## 13. Commit 规范

MathArts 默认使用 Conventional Commits 风格。详细要求参见[Commit 规范](https://github.com/matharts/ecosystem/blob/main/specs/git/commit.md)。目标仓库可以根据发布流程和工具链制定更具体的 Commit 要求。

基本格式：

```text
<类型>(<范围>): <简要描述>
```

示例：

```text
feat(bagua): 增加先天八卦方位映射
fix(calendar): 修正跨日边界判断
docs(contributing): 补充任务认领流程
test(tiangan): 增加五合条件回归测试
data(glossary): 补充术语来源
```

常用类型：

| 类型         | 用途           |
| ---------- | ------------ |
| `feat`     | 新功能          |
| `fix`      | Bug 修复       |
| `docs`     | 文档           |
| `refactor` | 不改变外部行为的重构   |
| `test`     | 测试           |
| `data`     | 知识数据或映射数据    |
| `perf`     | 性能优化         |
| `ci`       | CI/CD        |
| `chore`    | 构建、依赖或其他维护工作 |
| `style`    | 不影响逻辑的格式调整   |

不兼容变更必须明确标记：

```text
BREAKING CHANGE: 描述受影响的接口、使用者和迁移方式
```

Commit 历史应当便于理解和审查。维护者可以在合并前要求整理明显混乱的 Commit。

## 14. 提交 Pull Request

提交前，请同步目标仓库的最新默认分支，并解决由当前改动引起的冲突。

Pull Request 应说明：

* 解决的问题；
* 为什么需要这项改动；
* 主要修改范围；
* 设计、规范或规则依据；
* 测试和验证方式；
* 文档变化；
* 公共接口和兼容性影响；
* 是否包含 Breaking Change；
* 相关 Issue、Discussion、RFC 或 ADR；
* 尚未解决的问题和已知限制。

使用以下关键字可以在合并时自动关闭相关 Issue：

```text
Closes #123
Fixes #123
Resolves #123
```

工作尚未完成但希望提前获得反馈时，可以创建 Draft Pull Request，并明确列出未完成事项。

## 15. Pull Request 自检

提交评审前，请确认：

* [ ] 改动范围集中，没有混入无关修改；
* [ ] 已阅读目标仓库的本地贡献规则；
* [ ] 已运行与改动相关的测试和检查；
* [ ] 新增或修改行为已有适当测试；
* [ ] 无法自动测试的部分已提供人工验证步骤；
* [ ] 公共 API、配置和数据格式变化已经说明；
* [ ] Breaking Change 已明确标记并提供迁移方式；
* [ ] 相关文档已经同步更新；
* [ ] 新增术语或知识数据已说明来源和适用范围；
* [ ] 没有提交密钥、凭证、个人数据或无关生成文件；
* [ ] Commit 信息符合项目约定；
* [ ] 已关联相关 Issue，或说明为什么不需要 Issue。

并非每一项都适用于所有 Pull Request。对不适用的检查项，可以在 Pull Request 中简要说明。

## 16. 评审

评审的目标是提高正确性、可维护性和长期兼容性，而不是评价贡献者个人。

评审通常关注：

* 改动是否解决了所述问题；
* 实现和边界条件是否正确；
* 是否符合项目职责和已接受的决策；
* 测试是否足以发现回归；
* 术语、规则和数据是否可追溯；
* 公共接口是否必要且清晰；
* 是否存在兼容性、安全或性能风险；
* 文档是否与实现同步；
* 后续维护成本是否合理。

贡献者应当：

* 回应尚未解决的评审意见；
* 对不同意的建议说明技术理由；
* 在修改后标明需要重新检查的部分；
* 避免在评审过程中扩大 Pull Request 范围；
* 不通过私下施压、重复催促或人身评价推动合并。

维护者应提供具体、可执行且与当前改动相关的反馈。

## 17. 合并条件

Pull Request 通常需要满足：

* 改动范围和设计已经获得必要评审；
* 必需的自动化检查通过；
* 阻塞性评审意见已经解决；
* 测试和文档与改动相匹配；
* 兼容性和 Breaking Change 已充分说明；
* 不违反安全策略和行为准则；
* 获得目标仓库有权合并该改动的维护者批准。

目标仓库的分支保护规则和本地治理文件可以规定更严格的条件。

提交或评审贡献不代表改动必须被合并。维护者可以基于项目范围、质量、兼容性、维护成本或既有决策拒绝改动，但应尽可能说明理由。

## 18. 修改组织级社区文件

对 `matharts/.github` 仓库的修改可能影响多个组织仓库，因此需要额外说明：

* 变更影响哪些默认社区文件；
* 哪些仓库可能继承该变化；
* 项目仓库的本地文件是否会覆盖该变化；
* 是否改变贡献者现有流程；
* 是否需要同步修改 Issue 或 Pull Request 模板；
* 是否会产生重复或冲突的事实来源；
* 是否需要迁移说明。

项目特定的构建命令、架构事实和领域实现细节不应放入组织级默认文件。

## 19. 自动化和 AI 辅助内容

可以使用自动化工具或 AI 工具辅助编写代码、测试和文档，但工具不能代替贡献者承担责任。

贡献者必须：

* 理解并能够解释所提交的内容；
* 验证代码、引用、测试和资料来源；
* 检查生成内容是否包含错误、虚构信息或不兼容许可内容；
* 不向第三方工具提交密钥、未公开漏洞或受限制数据；
* 根据评审需要说明重要内容的生成和验证方式。

未经验证的大量生成内容可能被要求缩小范围、补充依据或重新提交。

## 20. 许可

除非目标仓库另有明确说明，你提交的贡献将按照该仓库当前采用的开源许可证发布。

你必须有权提交相关代码、文档、数据和其他材料。不得提交：

* 未获授权复制的代码或文本；
* 许可条件与目标仓库不兼容的内容；
* 无法确认来源的大段第三方材料；
* 包含个人数据、秘密信息或受限制资料的内容。

引用典籍、论文或其他资料时，应尽量只使用支持实现和验证所必需的内容，并清楚标注来源。

## 21. 行为准则

所有参与者都必须遵守 MathArts 的[社区行为准则](https://github.com/matharts/.github/blob/main/CODE_OF_CONDUCT.md)。

技术分歧应通过证据、测试、规范和清晰论证处理。不得将算法、术语或流派分歧转化为人身攻击，也不得利用行为准则压制善意的技术质疑。

## 22. 文件维护

本文件由 MathArts 组织维护者负责维护。

出现以下情况时应当复审：

* Issue 或 Pull Request 模板发生变化；
* 建立或修改统一的 RFC、ADR 或治理流程；
* Commit 或代码风格规范发生变化；
* 组织级角色和评审权限发生变化；
* 发布、兼容性或安全流程发生变化；
* 实际贡献过程中反复出现阻塞或歧义；
* GitHub 组织级默认社区文件的使用方式发生变化。

项目仓库如有不同的开发环境、测试命令、发布要求或评审规则，应建立自己的 `CONTRIBUTING.md` 并链接本文件，而不是复制后长期维护多个互相冲突的版本。
