# MathArts 安全策略

MathArts 重视开源软件、发布产物、自动化流程和用户数据的安全。

本文件说明 MathArts 项目的安全支持范围、漏洞报告方式、处理流程和协同披露原则。

本策略是 MathArts 组织级默认安全策略。组织内仓库未提供自己的 `SECURITY.md` 时，适用本文件；项目仓库提供了独立安全策略时，以项目仓库的规定为准。

## 1. 受支持版本

不同 MathArts 项目的成熟度、发布方式和维护周期可能不同。项目仓库应当在自己的文档中明确具体支持范围。

在没有项目特定说明时，默认采用以下规则：

| 项目版本或状态                | 安全支持                  |
| ---------------------- | --------------------- |
| 最新稳定版本                 | 支持                    |
| 默认分支                   | 接受安全报告，但默认分支不一定适合生产使用 |
| 明确标记为仍受支持的旧版本          | 支持，以项目仓库说明为准          |
| Alpha、Beta、RC 或其他预发布版本 | 接受报告，修复和兼容性按尽力原则处理    |
| 实验性项目或原型               | 接受报告，不承诺稳定性或修复周期      |
| 已弃用版本                  | 通常不再提供安全修复            |
| 已归档仓库                  | 通常不再提供安全修复            |

即使某个版本已不受支持，我们仍欢迎报告可能影响当前版本、其他 MathArts 项目或软件供应链的漏洞。

对于不再支持的版本，维护者可以选择：

* 提供升级建议或临时缓解措施；
* 将修复回移至旧版本；
* 发布安全公告但不提供补丁；
* 建议停止使用相关版本。

上述处理取决于漏洞影响、修复成本和维护资源，不构成持续支持承诺。

## 2. 哪些问题应当私密报告

以下问题通常应当作为安全漏洞私密报告：

* 任意代码执行；
* 命令注入、代码注入或模板注入；
* 路径遍历或未授权文件访问；
* 身份验证或权限检查绕过；
* 未授权读取、修改或删除数据；
* 敏感信息、访问令牌、密钥或凭证泄露；
* 可被利用的反序列化问题；
* 跨站脚本、跨站请求伪造或服务端请求伪造；
* SQL、NoSQL 或其他查询注入；
* 可造成显著影响的拒绝服务；
* 不安全的默认配置；
* 发布包、构建产物或依赖供应链被篡改；
* GitHub Actions、发布流程或自动化凭证被不当利用；
* 软件包名称抢注、依赖混淆或构建来源伪造；
* 加密、签名、随机数或密钥处理中的安全缺陷；
* 可能破坏数据机密性、完整性或可用性的其他问题。

当你无法确定问题是否属于安全漏洞时，建议优先进行私密报告。

## 3. 哪些问题通常不是安全漏洞

以下问题通常应通过普通 Issue、Discussion 或项目贡献流程提交：

* 一般功能错误；
* 算法、历法、术语或数术规则的正确性争议；
* 计算结果与某个流派、典籍或实现不一致；
* 文档错误、错别字或表述不清；
* 不涉及安全边界的性能问题；
* 缺少某项安全增强功能，但不存在可利用缺陷；
* 仅影响已明确弃用且无法影响当前版本的问题；
* 仅存在于上游依赖、且 MathArts 没有扩大其影响的问题；
* 社区行为、骚扰或其他人员互动问题。

算法和领域模型问题应按普通 Bug、Specification、RFC 或研究讨论处理。社区行为问题应按照 `CODE_OF_CONDUCT.md` 报告。

如果普通 Bug 同时可能导致未授权访问、敏感数据泄露、供应链攻击或其他安全影响，请改用私密安全报告。

## 4. 如何报告漏洞

### 4.1 首选：GitHub 私密漏洞报告

如果受影响仓库的 **Security** 页面提供 **Report a vulnerability** 入口，请优先通过该入口提交。

通过目标仓库报告有助于维护者准确识别受影响项目，并在私密环境中讨论、验证和修复问题。

### 4.2 备用：电子邮件

如果目标仓库没有提供私密漏洞报告入口，请发送邮件至：

**[lzm0x219@gmail.com](mailto:lzm0x219@gmail.com)**

建议使用以下邮件标题：

```text
[MathArts Security] 仓库名称：漏洞摘要
```

例如：

```text
[MathArts Security] calendar-core：路径遍历可能导致任意文件读取
```

### 4.3 不得公开报告

在维护者完成初步评估和必要保护措施前，请不要通过以下渠道披露尚未修复的漏洞细节：

* 公开 GitHub Issue；
* Pull Request；
* GitHub Discussion；
* 社交媒体；
* 聊天群组或论坛；
* 博客、视频或演示；
* 可公开访问的漏洞利用代码仓库。

如果安全问题已被公开，请仍然通过私密渠道通知维护者，并提供公开内容的位置。

## 5. 报告应包含的信息

请尽可能提供：

* 受影响的仓库、软件包或服务；
* 受影响的版本、提交或发布产物；
* 漏洞类型和简要说明；
* 可能的攻击场景；
* 复现步骤；
* 最小化验证代码或概念验证；
* 预期行为和实际行为；
* 潜在影响；
* 是否需要身份验证或特殊配置；
* 已知的临时缓解方式；
* 你是否已经向上游依赖或其他项目报告；
* 你期望采用的披露时间；
* 是否希望在安全公告中获得署名。

请不要发送与漏洞验证无关的个人数据、生产数据、访问凭证或其他敏感材料。

如需提供较大的附件、敏感日志或可能产生实际破坏的利用代码，请先发送摘要，与维护者确认安全的传输方式。

## 6. 凭证和密钥泄露

如果你发现 MathArts 的令牌、密码、私钥、发布凭证或其他秘密信息已经泄露：

1. 不要继续使用该凭证；
2. 不要在公开 Issue 中粘贴凭证内容；
3. 立即通过私密渠道报告；
4. 提供发现位置，但尽量不要在邮件中重复完整秘密；
5. 如果你有权限，应优先撤销或轮换相关凭证；
6. 不要使用泄露的凭证访问其原本无权访问的资源。

删除公开内容并不能保证秘密已经安全。已公开的凭证通常应当视为已经泄露并进行轮换。

## 7. 善意安全研究

MathArts 欢迎以保护用户和改善项目安全为目的的善意研究。

在自身权限范围内，MathArts 不会仅因符合本节要求的善意研究而对研究者采取报复性措施。

研究者必须：

* 避免损害数据、系统和服务；
* 只进行确认漏洞所必需的最小测试；
* 不持续访问、下载或修改他人数据；
* 不使用社会工程学、钓鱼或人身欺骗；
* 不进行可能影响其他用户的拒绝服务测试；
* 不尝试获取或维持持久访问权限；
* 不访问超出漏洞验证所必需的系统；
* 发现敏感数据后立即停止进一步访问；
* 及时通过私密渠道报告；
* 为维护者提供合理的修复和缓解时间；
* 遵守适用法律和第三方平台规则。

本策略不授权测试不属于 MathArts 控制的第三方系统、基础设施或账号，也不能代表第三方向研究者提供法律豁免。

如果不确定某项测试是否安全或是否获得授权，请先提供研究计划，不要直接执行可能造成影响的操作。

## 8. MathArts 的处理流程

收到报告后，维护者通常会按照以下流程处理：

### 8.1 接收确认

维护者将确认报告已经收到，并检查是否需要立即采取以下措施：

* 撤销泄露凭证；
* 暂停受影响的发布或自动化流程；
* 隐藏已经公开的敏感信息；
* 限制受影响功能；
* 通知相关仓库维护者。

我们的目标是在 **7 个自然日内**确认收到报告。

### 8.2 初步评估

维护者将判断：

* 问题是否属于安全漏洞；
* 哪些项目和版本受到影响；
* 是否能够复现；
* 是否存在正在发生的利用；
* 是否需要上游项目或平台参与；
* 是否需要临时缓解措施；
* 是否需要发布安全公告。

我们的目标是在确认报告后的 **14 个自然日内**提供初步评估或进度说明。

上述时间是开源维护目标，不构成服务等级协议。复杂问题、维护者不可用或跨项目协调可能需要更长时间。

### 8.3 修复与验证

确认漏洞后，维护者可以：

* 在私密分支或 Security Advisory 中开发修复；
* 邀请报告者协助验证；
* 修复受影响的默认分支；
* 根据支持范围决定是否回移至旧版本；
* 准备安全公告、升级说明和缓解措施；
* 检查相同缺陷是否存在于其他 MathArts 仓库。

报告者不得因为协助验证而获得目标仓库的常规写入或发布权限。临时权限应遵循最小必要原则。

### 8.4 发布与披露

完成修复后，维护者将根据实际情况：

* 发布修复版本；
* 更新 Changelog 或 Release Notes；
* 发布 GitHub Security Advisory；
* 提供受影响版本和修复版本；
* 说明升级或缓解方法；
* 在适当情况下申请或关联 CVE；
* 感谢报告者的贡献。

不是每个漏洞都会获得 CVE，也不是每个修复都会发布独立安全公告。

## 9. 协同披露

MathArts 支持协调漏洞披露。

报告者和维护者应共同确定合理的公开时间。确定时间时应考虑：

* 漏洞严重程度；
* 是否存在公开利用；
* 修复和测试所需时间；
* 下游使用者升级所需时间；
* 是否涉及上游依赖或多个项目；
* 是否需要撤销凭证或迁移基础设施。

MathArts 不会要求报告者无限期保持沉默。若处理明显停滞，报告者应先通过原报告渠道请求状态更新，并在公开前给予合理通知。

以下情况可能需要提前披露或发布紧急缓解信息：

* 已发现漏洞正在被利用；
* 漏洞细节已经公开；
* 泄露凭证仍然有效；
* 延迟披露会显著增加用户风险。

公开披露前，双方应尽可能避免发布可直接用于攻击尚未升级用户的完整利用细节。

## 10. 严重程度判断

MathArts 将综合考虑以下因素，而不是只依据单一评分：

* 利用难度；
* 是否需要身份验证；
* 攻击者需要的权限；
* 受影响用户和项目范围；
* 数据机密性、完整性和可用性影响；
* 是否影响发布或软件供应链；
* 是否能够远程利用；
* 是否存在公开利用代码；
* 是否已经发生实际利用；
* 是否存在有效缓解措施；
* 受影响版本的成熟度和使用场景。

必要时可以参考 CVSS，但 CVSS 分数不自动决定修复优先级或支持范围。

## 11. 上游依赖漏洞

如果问题完全位于第三方依赖中，请优先按照上游项目的安全策略报告。

同时满足以下任一条件时，也请通知 MathArts：

* MathArts 固定使用了受影响版本；
* MathArts 的默认配置使漏洞可被直接利用；
* MathArts 对依赖的使用方式扩大了漏洞影响；
* 当前没有可用的安全升级路径；
* MathArts 的发布产物包含受影响组件；
* 上游修复需要 MathArts 进行兼容性调整。

请不要在 MathArts 的公开 Issue 中披露尚未公开的上游漏洞。

## 12. 报告者署名

在报告者同意且不会增加风险的情况下，MathArts 可以在安全公告或 Release Notes 中感谢报告者。

报告者可以选择：

* 使用真实姓名；
* 使用 GitHub 用户名或其他公开身份；
* 代表所属安全团队署名；
* 匿名。

署名不表示 MathArts 对报告者、其组织、产品或服务作出认可。

除非另有书面说明，漏洞报告不会自动产生漏洞赏金、报酬或其他奖励承诺。

## 13. 保密和信息访问

安全报告应仅向处理事件所必需的人员开放。

参与处理的维护者应当：

* 避免在公开频道讨论未修复漏洞；
* 不向无关人员转发报告材料；
* 只授予完成修复所需的最小权限；
* 在不再需要时撤销临时访问权限；
* 避免在测试、日志和构建产物中复制真实敏感数据；
* 在发布公告前与相关项目协调。

MathArts 无法承诺绝对保密。法律义务、平台规则或紧急用户保护可能要求披露部分信息，但维护者应尽可能限制披露范围。

## 14. 利益冲突

如果安全报告涉及：

* 负责接收报告的人员；
* 仓库主要维护者；
* 组织所有者；
* 其他可能影响公正处理的人员；

相关人员不得单独决定报告是否成立、如何处置或是否公开。

报告者应在邮件标题或正文中注明“利益冲突”，并优先使用目标仓库的 GitHub 私密漏洞报告入口。事件应交由未涉事且具有必要权限的维护者处理。

## 15. 安全事件与社区行为

安全漏洞报告用于处理软件、基础设施、发布流程和数据安全问题。

以下问题不应通过安全漏洞渠道处理：

* 骚扰、威胁或歧视；
* 维护者滥用社区管理权限；
* 人身攻击；
* 隐私侵害但不涉及软件安全缺陷；
* 其他违反社区行为准则的行为。

这些事件应按照 `CODE_OF_CONDUCT.md` 报告。

如果事件同时涉及软件安全和社区安全，可以分别通过两个渠道报告，并说明两者之间的关系。

## 16. 策略维护

本文件由 MathArts 组织维护者负责维护。

出现以下情况时应当复审：

* 安全报告邮箱或负责人发生变化；
* 启用或停用 GitHub 私密漏洞报告；
* 项目版本策略发生变化；
* 发布、签名或软件供应链流程发生变化；
* 新增生产服务或用户数据处理能力；
* 实际安全事件暴露出流程缺口；
* 组织治理和维护者权限发生变化。

项目仓库如有不同的发布周期、支持版本、响应人员或安全边界，应维护自己的 `SECURITY.md`，并链接本组织级策略，而不是复制后形成多个互相冲突的版本。
